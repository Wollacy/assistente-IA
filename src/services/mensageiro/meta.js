async function enviar(numero, mensagem) {
  // Futuro: implementar chamada para WhatsApp Cloud API oficial
  console.log(`🔧 WhatsApp Cloud API (Meta) ainda não implementado`);
  return;
}

module.exports = { enviar };
